# Have I got everything (HIGE) client

A Svelte SPA for Have I got everything app.

## Local development

Copy `.env.example` to `.env` and fill the data out.

Install dependencies:

```bash
yarn
```

Run the app:

```bash
yarn dev
```
